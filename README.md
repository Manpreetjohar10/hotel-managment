# MERN Hotel Scaffold

This repository contains a minimal MERN scaffold for a hotel listing app.

Folders:
- `backend` — Express API with Mongoose models.
- `frontend` — React app (CRA-style) that consumes the API.

Follow the READMEs in each folder to run them.
