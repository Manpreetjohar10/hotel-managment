# MERN Hotel — Frontend

Minimal React app to list hotels from backend API.

Quick start:

1. In `frontend` run `npm install`.
2. Start the dev server with `npm start`.

The frontend proxies API requests to the backend using `setupProxy.js`.
